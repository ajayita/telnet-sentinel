name: telnet_sentinel
description: A diagnostic and auditing framework for bridging ancient Telnet communication standards with modern software reliability.
version: 0.0.1

environment:
  sdk: ^3.11.5

dependencies:
  args: ^2.7.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
